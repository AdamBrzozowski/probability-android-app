<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BinomialActivity"></activity>
        <activity android:name=".PoissonActivity" />
        <activity android:name=".PowerActivity" />
        <activity android:name=".CellularNetworksActivity" />
        <activity android:name=".IntroActivity" />
        <activity
            android:name=".ErlangActivity"
            android:label="ErlangB"
            android:screenOrientation="portrait" />
        <activity
            android:name=".OutageActivity"
            android:label="@string/outage_activity_name"
            android:screenOrientation="portrait" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>